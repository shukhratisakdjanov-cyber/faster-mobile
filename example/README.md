# Example app integration

This directory holds the integrated usage screen. It will be connected to a generated React Native host application in the next milestone so the package is consumed solely through its public `@shukhratisakdjanov-cyber/faster-mobile` exports.
